package Java_Practice;

public class Polymorphism {

	public static void main(String[] args) {
	// Polymorphism poly means many and morphs means forms many forms
	//in overloading the decision taken at compile time (which method has to execute) so this is called compile time polymorphism
	//In overriding the decision taken at run time (which method has to execute) so this is called run time polymorphism, dynamic poly
    // In overriding complier will confuse which method have to execute so JVM will take care of execution(child class will execute)
	}

}
